# Overture — driven test run, 2026-09-24

Driven by fo4-mcp against the plan in `docs/test-plan-drivable.md`. Every NPC line is judged by
its TESTopicInfo id against `build/voice-registry.json` -> `voice/lines.json`, never by text alone.
This file is generated from the run's own `results.jsonl`; the numbers are not transcribed by hand.

## Build under test
- `Overture.esp` sha256 `61350d05612e48ee…`, 170,747 bytes, mtime 2026-09-24 01:25:48 — the plan's step-0 anchor, unchanged.
- `Sound/Voice/Overture.esp`: 1,860 .fuz.
- `Scripts/Overture/Approach.pex` was restaged mid-run (16:27). It does not invalidate anything here:
  `git diff f362cdd..HEAD -- papyrus/Overture/Approach.psc` is 151 insertions, 0 deletions, one hunk
  after the file's last function, adding only Debug* functions called solely from DebugTriggers.psc.

## Results

| stage | route | persona | sex | cells | pass | fail |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | reset/fresh | mercantile | f | 8 | 8 | 0 |
| 1 | reset/fresh | reticent | m | 8 | 8 | 0 |
| 1 | reset/fresh | romantic | f | 8 | 8 | 0 |
| 1 | reset/fresh | vulgar | m | 8 | 8 | 0 |
| 1 | reset/fresh | vulgar | f | 1 | 1 | 0 |
| 2 | reset/fresh | mercantile | f | 3 | 3 | 0 |
| 2 | reset/fresh | vulgar | m | 8 | 8 | 0 |
| 2 | continuous | mercantile | f | 1 | 1 | 0 |
| 2 | continuous | romantic | f | 1 | 1 | 0 |
| 2 | continuous | vulgar | m | 1 | 1 | 0 |
| | | **total** | | **47** | **47** | **0** |

### What each persona lands on

| persona | lands on | private blunt | public blunt |
| --- | --- | --- | --- |
| mercantile | `offer` | miss | recoil |
| romantic | `charm` | miss | recoil |
| vulgar | `blunt` | land | recoil |
| reticent | `linger` | miss | recoil |

## Verified
- **Stage 1, all four personas, both rooms.** Each persona lands only on its own register and
  misses the other three. `blunt` in a public room draws that persona's recoil; in a private room
  it never does. A land opens the stage-2 wheel for every persona except the reticent, which hands
  back — R-8, by design. The Narrator line fires, and the hand-back returns to the NPC's own dialogue.
- **O-8, once a day.** After a conversation the second `activate` opens the NPC's own dialogue and
  `approach status` reads `closed until day N`.
- **O-40c, both sexes, stages 1 and 2.** 12 repeat draws of the vulgar land cell: the male drew
  `land_m1` 2/6, the female `land_f1` 3/6, and neither ever drew the other's line. Gendered stage-2
  lines (`ov2_vulgar_*_m1`) also came only to the male.
- **Both routes into stage 2 agree.** A fresh greeting after `approach reset` and staying in the same
  conversation through the scene's phase branching draw the same stage-2 line for the same register.
- **O-5 / O-40c on the build.** All 344 registry infos resolve to a bank line; 68 are gendered and
  every one ships in exactly its own sex's three core voice types and no others. 1,860 .fuz total.

## NOT covered by this run
- the debug path (MCM Debug buttons and hotkeys) - a wheel-driven matrix never calls it; the adults-only CRITICAL fixed in that path could not have been caught by any of the 83 assertions
- AAF scene start - approach scenes off is asserted per branch, so nothing past saidYes=1
- audio: whether a line is HEARD; only the .fuz files' presence and placement were checked
- **Stage 3 (the proposition and its five verdicts), step 4 (lovers), steps 5-6, and step 7's in-game half.** Not started.
- Stage 2 for the reticent, and the remaining mercantile/romantic stage-2 cells. Partial.

## Defects found in Overture
None. Every captured line matched the bank on persona, stage, register, room and outcome.

The failures during this run were all in the test harness or the engine, not in Overture:
XDI refusing a choice until the player is squared up on the NPC; the engine wedging after many
in-session loads so no dialogue opens at all until a relaunch; one crash in the AI patrol path;
and an NPC (Audrey) whose own greeting outranks the approach, which is expected behaviour and is
now a candidate rule in the plan.
